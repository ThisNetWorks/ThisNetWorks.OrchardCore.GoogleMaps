@Model.Metadata.ChildContent

@* Inject the settings only once per page *@
@if (!Context.Items.ContainsKey("DisplayMap__Settings"))
{
    Context.Items["DisplayMap__Settings"] = new object();
    @await DisplayAsync(await New.DisplayMap__Settings())
}